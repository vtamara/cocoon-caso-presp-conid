<h1>Listing presponsables</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @presponsables.each do |presponsable| %>
      <tr>
        <td><%= presponsable.nombre %></td>
        <td><%= link_to 'Show', presponsable %></td>
        <td><%= link_to 'Edit', edit_presponsable_path(presponsable) %></td>
        <td><%= link_to 'Destroy', presponsable, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Presponsable', new_presponsable_path %>
